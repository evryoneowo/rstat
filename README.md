# rstat
light stat utility

## installation
1. `git clone https://github.com/evryoneowo/rstat && cd rstat`
2. `make`
3. `sudo make install`

## how to use
`rstat` (usual mode)
`rstat -l` (monitor mode)
`rstat -v` (version & info)
